{
  "name": "tracker_ts",
  "version": "1.0.0",
  "description": "",
  "main": "script.js",
  "scripts": {
    "start": "npm run build && http-server",
    "build": "rm -rf static/tracker* && esbuild --bundle --minify --format=iife --outfile=static/script.js src/main.ts",
    "test": "echo \"Error: no test specified\" && exit 1",
    "temp": "--global-name=_Tracker"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "esbuild": "^0.21.1",
    "typescript": "^5.4.5"
  },
  "devDependencies": {
    "http-server": "^14.1.1"
  }
}

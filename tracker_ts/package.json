{
  "name": "tracker_ts",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "rm -rf static/tracker* && esbuild --bundle --minify --global-name=_goTracker --format=iife --outfile=static/tracker.js src/tracker.ts",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "esbuild": "^0.21.1",
    "typescript": "^5.4.5"
  }
}
